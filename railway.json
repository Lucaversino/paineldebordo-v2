{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm ci --omit=dev"
  },
  "deploy": {
    "startCommand": "npm run ais:worker",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 120,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
